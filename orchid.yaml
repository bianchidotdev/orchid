
type: cluster
version: v0.1
name: orchid
controller: docker
nodes:
- localhost
# not needed if this is the only source file
sources:
- type: git
  url: https://git.bianchi.dev/orchid_test

---

# TODO: put name in metadata?
name: orchid-service
type: service
version: v0.1
  # NOTE: these will not actually be deployed as a pod, but rather are forced to be on a single node
containers:
- name: busybox
  image: busybox
  command: ["sleep", "3600"]
